import Iconify from '../../../components/iconify';

// ----------------------------------------------------------------------

const navConfig = [
  {
    title: 'Propietario',
    path: '/propietario',
    icon: <Iconify icon={"ph:key-fill"} />,  
  },
];

export default navConfig;