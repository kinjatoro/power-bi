version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build  # Cambia esto si el directorio de salida es diferente
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
